Flask==3.0.0
Flask-CORS==4.0.0
pyserial>=3.5
waitress==3.0.0
pywebview==5.3
pystray==0.19.5
Pillow>=10.0
qrcode>=7.4
cryptography>=42.0
openpyxl>=3.1
arabic-reshaper>=3.0
python-bidi>=0.4
# Encryption-at-rest (spike-validated 2026-07-09, see .superpowers/sdd/task-1-report.md):
# sqlcipher3-binary and pysqlcipher3 (named in the original spec) have NO Windows
# wheels at all (manylinux-only / sdist-only, no MSVC toolchain available to build
# pysqlcipher3 from source). `sqlcipher3` (coleifer/laggykiller fork) ships
# win_amd64 wheels cp310-cp314 with SQLCipher+OpenSSL statically linked into the
# .pyd -- use this import name instead in all subsequent tasks.
sqlcipher3>=0.6.2
# pywin32 has no Linux distribution at all ("from versions: none" on pip) --
# marker-gated so `pip install -r requirements.txt` still succeeds on the
# ubuntu-latest CI runner. sqlcipher3 itself ships real manylinux wheels and
# runs fine there; only the DPAPI key-wrapping layer (encryption_key.py) is
# Windows-only, and tests/conftest.py stands that in on non-Windows.
pywin32>=306; sys_platform == 'win32'
